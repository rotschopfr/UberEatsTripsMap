name: Transcribe Raff Ep93 (completed)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Full Ep.93 transcription already completed; workflow parked.'
